apiVersion: v1
kind: Service
metadata:
  name: {{ include "myapp.fullname" . }}
spec:
  type: LoadBalancer
  selector:
    app: {{ include "myapp.fullname" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
